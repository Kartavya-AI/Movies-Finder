{
  "mcpServers": {
    "TVRecommender": {
      "command": "npx",
      "args": [
        "run",
        "--rm",
        "-e", "IMDB_API_KEY=${TMDB_API_KEY}",
        "-e", "OPENAI_API_KEY=${OPENAI_API_KEY}",
        "-e", "LANGSMITH_API_KEY=${LANGSMITH_API_KEY}",
        "-e", "LANGUAGE=en-US",
        "tv-recommender-mcp-server"
      ]
    }
  }
}
